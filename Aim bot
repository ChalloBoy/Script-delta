-- [[ MOBILE AIM ASSIST + RGB ESP ]]
-- [[ FOR YOUR OWN ROBLOX EXPERIENCE ]]
-- [[ CHALLO BOY X RENA STUDIO ]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

--==================================================
-- UI SETUP
--==================================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MobileAimMenu"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MenuOpenButton = Instance.new("TextButton")
MenuOpenButton.Name = "MenuOpenButton"
MenuOpenButton.Parent = ScreenGui
MenuOpenButton.BackgroundColor3 = Color3.fromRGB(35,35,40)
MenuOpenButton.Position = UDim2.new(0,10,0,40)
MenuOpenButton.Size = UDim2.new(0,85,0,32)
MenuOpenButton.Font = Enum.Font.SourceSansBold
MenuOpenButton.Text = "MENU"
MenuOpenButton.TextColor3 = Color3.fromRGB(255,255,255)
MenuOpenButton.TextSize = 14

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0,7)
BtnCorner.Parent = MenuOpenButton

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(25,25,30)
MainFrame.Position = UDim2.new(0.5,-105,0.2,0)
MainFrame.Size = UDim2.new(0,210,0,235)
MainFrame.Visible = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0,8)
MainCorner.Parent = MainFrame

--==================================================
-- TOP BAR
--==================================================

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(40,40,45)
TopBar.Size = UDim2.new(1,0,0,34)

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0,8)
TopCorner.Parent = TopBar

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1,0,1,0)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "AIM MENU"
Title.TextColor3 = Color3.fromRGB(255,255,255)
Title.TextSize = 14

--==================================================
-- CONTENT
--==================================================

local ContentFrame = Instance.new("Frame")
ContentFrame.Parent = MainFrame
ContentFrame.BackgroundTransparency = 1
ContentFrame.Position = UDim2.new(0,0,0,43)
ContentFrame.Size = UDim2.new(1,0,1,-43)

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = ContentFrame
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIListLayout.Padding = UDim.new(0,7)

--==================================================
-- VARIABLES
--==================================================

local EspEnabled = false
local AimbotEnabled = false
local IsTouching = false
local AimStrength = 50

--==================================================
-- RGB ESP
--==================================================

local function GetRGB()
	return Color3.fromHSV((tick() * 0.15) % 1,1,1)
end

local function UpdateESPColor(character)

	local esp = character:FindFirstChild("RGB_ESP")

	if esp then
		esp.FillColor = GetRGB()
		esp.OutlineColor = GetRGB()
	end

end

--==================================================
-- TOGGLE CREATOR
--==================================================

local function CreateToggle(name,defaultState,callback)

	local Button = Instance.new("TextButton")
	Button.Parent = ContentFrame
	Button.Size = UDim2.new(0,180,0,38)
	Button.Font = Enum.Font.SourceSansBold
	Button.TextSize = 15
	Button.TextColor3 = Color3.fromRGB(255,255,255)
	Button.BackgroundColor3 = Color3.fromRGB(50,50,55)

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0,6)
	Corner.Parent = Button

	local state = defaultState

	local function Update()

		if state then
			Button.BackgroundColor3 = Color3.fromRGB(55,120,75)
			Button.Text = name .. ": ON"
		else
			Button.BackgroundColor3 = Color3.fromRGB(50,50,55)
			Button.Text = name .. ": OFF"
		end

	end

	Update()

	Button.MouseButton1Click:Connect(function()

		state = not state
		Update()
		callback(state)

	end)

end

--==================================================
-- WALL / LINE OF SIGHT CHECK
--==================================================

local function IsVisible(targetPart)

	if not LocalPlayer.Character then
		return false
	end

	local raycastParams = RaycastParams.new()

	raycastParams.FilterDescendantsInstances = {
		LocalPlayer.Character
	}

	raycastParams.FilterType = Enum.RaycastFilterType.Exclude

	local origin = Camera.CFrame.Position
	local direction = targetPart.Position - origin

	local result = workspace:Raycast(
		origin,
		direction,
		raycastParams
	)

	-- Tidak ada objek di antara kamera dan target
	if not result then
		return true
	end

	-- Ray harus mengenai bagian tubuh target
	if result.Instance:IsDescendantOf(targetPart.Parent) then
		return true
	end

	return false

end

--==================================================
-- ESP ENGINE
--==================================================

local function ApplyESP(player)

	if player == LocalPlayer then
		return
	end

	local function SetupCharacter(character)

		local oldESP = character:FindFirstChild("RGB_ESP")

		if oldESP then
			oldESP:Destroy()
		end

		local Highlight = Instance.new("Highlight")

		Highlight.Name = "RGB_ESP"
		Highlight.Parent = character
		Highlight.FillTransparency = 0.65
		Highlight.OutlineTransparency = 0
		Highlight.Enabled = EspEnabled
		Highlight.FillColor = GetRGB()
		Highlight.OutlineColor = GetRGB()

	end

	if player.Character then
		SetupCharacter(player.Character)
	end

	player.CharacterAdded:Connect(SetupCharacter)

end

for _,player in ipairs(Players:GetPlayers()) do
	ApplyESP(player)
end

Players.PlayerAdded:Connect(ApplyESP)

--==================================================
-- ESP TOGGLE
--==================================================

CreateToggle("ESP",false,function(state)

	EspEnabled = state

	for _,player in ipairs(Players:GetPlayers()) do

		if player.Character then

			local esp = player.Character:FindFirstChild("RGB_ESP")

			if esp then
				esp.Enabled = state
			end

		end

	end

end)

--==================================================
-- AIM TARGET
--==================================================

local function GetClosestPlayer()

	local closestPlayer = nil
	local shortestDistance = math.huge

	if not LocalPlayer.Character then
		return nil
	end

	local myRoot =
		LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

	if not myRoot then
		return nil
	end

	local viewportCenter = Vector2.new(
		Camera.ViewportSize.X / 2,
		Camera.ViewportSize.Y / 2
	)

	for _,player in ipairs(Players:GetPlayers()) do

		if player ~= LocalPlayer
		and player.Character then

			local humanoid =
				player.Character:FindFirstChildOfClass("Humanoid")

			local head =
				player.Character:FindFirstChild("Head")

			if humanoid
			and humanoid.Health > 0
			and head then

				local screenPosition,onScreen =
					Camera:WorldToViewportPoint(head.Position)

				if onScreen and IsVisible(head) then

					local screenDistance =
						(Vector2.new(
							screenPosition.X,
							screenPosition.Y
						) - viewportCenter).Magnitude

					if screenDistance < shortestDistance then

						shortestDistance = screenDistance
						closestPlayer = player

					end

				end

			end

		end

	end

	return closestPlayer

end

--==================================================
-- AIM TOGGLE
--==================================================

CreateToggle("Aim Assist",false,function(state)

	AimbotEnabled = state

end)

--==================================================
-- AIM STRENGTH
--==================================================

local StrengthLabel = Instance.new("TextLabel")
StrengthLabel.Parent = ContentFrame
StrengthLabel.Size = UDim2.new(0,180,0,25)
StrengthLabel.BackgroundTransparency = 1
StrengthLabel.Font = Enum.Font.SourceSansBold
StrengthLabel.TextSize = 14
StrengthLabel.TextColor3 = Color3.fromRGB(255,255,255)
StrengthLabel.Text = "Strength: 50%"

local Slider = Instance.new("Frame")
Slider.Parent = ContentFrame
Slider.Size = UDim2.new(0,180,0,18)
Slider.BackgroundColor3 = Color3.fromRGB(55,55,60)

local SliderCorner = Instance.new("UICorner")
SliderCorner.CornerRadius = UDim.new(1,0)
SliderCorner.Parent = Slider

local Fill = Instance.new("Frame")
Fill.Parent = Slider
Fill.Size = UDim2.new(0.5,0,1,0)
Fill.BackgroundColor3 = Color3.fromRGB(100,100,110)

local FillCorner = Instance.new("UICorner")
FillCorner.CornerRadius = UDim.new(1,0)
FillCorner.Parent = Fill

local SliderDragging = false

local function SetStrength(x)

	local percent = math.clamp(
		(x - Slider.AbsolutePosition.X)
		/ Slider.AbsoluteSize.X,
		0,
		1
	)

	AimStrength = math.floor(percent * 100)

	Fill.Size = UDim2.new(percent,0,1,0)

	StrengthLabel.Text =
		"Strength: " .. AimStrength .. "%"

end

Slider.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then

		SliderDragging = true
		SetStrength(input.Position.X)

	end

end)

Slider.InputEnded:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then

		SliderDragging = false

	end

end)

UserInputService.InputChanged:Connect(function(input)

	if SliderDragging then

		if input.UserInputType == Enum.UserInputType.Touch
		or input.UserInputType == Enum.UserInputType.MouseMovement then

			SetStrength(input.Position.X)

		end

	end

end)

--==================================================
-- TOUCH / MOUSE
--==================================================

UserInputService.InputBegan:Connect(function(input,processed)

	if processed then
		return
	end

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then

		IsTouching = true

	end

end)

UserInputService.InputEnded:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then

		IsTouching = false

	end

end)

--==================================================
-- AIM LOOP
--==================================================

RunService.RenderStepped:Connect(function()

	-- RGB ESP ONLY
	for _,player in ipairs(Players:GetPlayers()) do

		if player ~= LocalPlayer
		and player.Character then

			UpdateESPColor(player.Character)

		end

	end

	-- AIM ASSIST
	if AimbotEnabled and IsTouching then

		local target = GetClosestPlayer()

		if target
		and target.Character then

			local head =
				target.Character:FindFirstChild("Head")

			if head and IsVisible(head) then

				local strength =
					math.clamp(AimStrength / 100,0,1)

				if strength > 0 then

					local cameraPosition =
						Camera.CFrame.Position

					local targetCFrame =
						CFrame.new(
							cameraPosition,
							head.Position
						)

					Camera.CFrame =
						Camera.CFrame:Lerp(
							targetCFrame,
							strength
						)

				end

			end

		end

	end

end)

--==================================================
-- MENU TOGGLE
--==================================================

MenuOpenButton.MouseButton1Click:Connect(function()

	MainFrame.Visible = not MainFrame.Visible

end)

--==================================================
-- MOBILE DRAG
--==================================================

local dragging = false
local dragStart
local startPosition

TopBar.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then

		dragging = true
		dragStart = input.Position
		startPosition = MainFrame.Position

		input.Changed:Connect(function()

			if input.UserInputState ==
				Enum.UserInputState.End then

				dragging = false

			end

		end)

	end

end)

UserInputService.InputChanged:Connect(function(input)

	if not dragging then
		return
	end

	if input.UserInputType ~= Enum.UserInputType.Touch
	and input.UserInputType ~= Enum.UserInputType.MouseMovement then
		return
	end

	local delta = input.Position - dragStart

	MainFrame.Position = UDim2.new(
		startPosition.X.Scale,
		startPosition.X.Offset + delta.X,
		startPosition.Y.Scale,
		startPosition.Y.Offset + delta.Y
	)

end)
