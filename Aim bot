--// CHALLO_BOY - AIM ASSIST + ESP OUTLINE
--// Versi FIXED: FOV slider + FOV ON/OFF + Minimize + Touch

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

--==================================================
-- SETTINGS
--==================================================

local AimEnabled = true
local ESPEnabled = true
local FOVEnabled = true

local AimStrength = 0.70
local FOVSize = 180

local MinFOV = 60
local MaxFOV = 400
local MaxDistance = 500

--==================================================
-- GUI
--==================================================

local Gui = Instance.new("ScreenGui")
Gui.Name = "ChalloBoy_AimAssist"
Gui.ResetOnSpawn = false
Gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

--==================================================
-- FOV CIRCLE
--==================================================

local FOV = Instance.new("Frame")
FOV.Name = "FOVCircle"
FOV.AnchorPoint = Vector2.new(0.5, 0.5)
FOV.Position = UDim2.fromScale(0.5, 0.5)
FOV.Size = UDim2.fromOffset(FOVSize, FOVSize)
FOV.BackgroundTransparency = 1
FOV.Visible = true
FOV.Parent = Gui

local FOVCorner = Instance.new("UICorner")
FOVCorner.CornerRadius = UDim.new(1, 0)
FOVCorner.Parent = FOV

local FOVStroke = Instance.new("UIStroke")
FOVStroke.Thickness = 1.5
FOVStroke.Transparency = 0
FOVStroke.Parent = FOV

--==================================================
-- MAIN UI
--==================================================

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = UDim2.fromOffset(250, 365)
Main.Position = UDim2.fromOffset(25, 100)
Main.BackgroundColor3 = Color3.fromRGB(18, 18, 23)
Main.BorderSizePixel = 0
Main.Parent = Gui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 2
MainStroke.Parent = Main

--==================================================
-- TITLE
--==================================================

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -55, 0, 38)
Title.Position = UDim2.fromOffset(15, 8)
Title.BackgroundTransparency = 1
Title.Text = "⚡ AIM ASSIST"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextSize = 20
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local SubTitle = Instance.new("TextLabel")
SubTitle.Size = UDim2.new(1, -30, 0, 20)
SubTitle.Position = UDim2.fromOffset(15, 42)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "AIM • FOV • ESP OUTLINE"
SubTitle.TextColor3 = Color3.fromRGB(170,170,170)
SubTitle.TextSize = 11
SubTitle.Font = Enum.Font.Gotham
SubTitle.TextXAlignment = Enum.TextXAlignment.Left
SubTitle.Parent = Main

--==================================================
-- MINIMIZE
--==================================================

local Minimize = Instance.new("TextButton")
Minimize.Size = UDim2.fromOffset(35, 30)
Minimize.Position = UDim2.new(1, -43, 0, 10)
Minimize.BackgroundColor3 = Color3.fromRGB(35,35,42)
Minimize.Text = "—"
Minimize.TextColor3 = Color3.new(1,1,1)
Minimize.TextSize = 20
Minimize.Font = Enum.Font.GothamBold
Minimize.Parent = Main

local MinCorner = Instance.new("UICorner")
MinCorner.CornerRadius = UDim.new(0,8)
MinCorner.Parent = Minimize

--==================================================
-- DRAG
--==================================================

local dragging = false
local dragStart
local startPos

local function updateDrag(input)

	if not dragging then
		return
	end

	local delta = input.Position - dragStart

	Main.Position = UDim2.new(
		startPos.X.Scale,
		startPos.X.Offset + delta.X,
		startPos.Y.Scale,
		startPos.Y.Offset + delta.Y
	)
end

Main.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPos = Main.Position

		input.Changed:Connect(function()

			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end

		end)
	end
end)

UIS.InputChanged:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseMovement
	or input.UserInputType == Enum.UserInputType.Touch then

		updateDrag(input)
	end
end)

--==================================================
-- CONTENT LIST
--==================================================

local Content = {}

local function addContent(obj)
	table.insert(Content, obj)
end

--==================================================
-- TOGGLE CREATOR
--==================================================

local function createToggle(text, y, callback)

	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(1, -30, 0, 38)
	Button.Position = UDim2.fromOffset(15, y)
	Button.BackgroundColor3 = Color3.fromRGB(30,30,37)
	Button.Text = text
	Button.TextColor3 = Color3.new(1,1,1)
	Button.TextSize = 13
	Button.Font = Enum.Font.GothamBold
	Button.Parent = Main

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0,9)
	corner.Parent = Button

	addContent(Button)

	Button.MouseButton1Click:Connect(function()
		callback()
	end)

	return Button
end

--==================================================
-- AIM TOGGLE
--==================================================

local AimButton

AimButton = createToggle(
	"🎯 AIM ASSIST : ON",
	75,
	function()

		AimEnabled = not AimEnabled

		if AimEnabled then
			AimButton.Text = "🎯 AIM ASSIST : ON"
		else
			AimButton.Text = "🎯 AIM ASSIST : OFF"
		end
	end
)

--==================================================
-- ESP TOGGLE
--==================================================

local ESPButton

ESPButton = createToggle(
	"👁 ESP OUTLINE : ON",
	120,
	function()

		ESPEnabled = not ESPEnabled

		if ESPEnabled then
			ESPButton.Text = "👁 ESP OUTLINE : ON"
		else
			ESPButton.Text = "👁 ESP OUTLINE : OFF"
		end
	end
)

--==================================================
-- FOV TOGGLE
--==================================================

local FOVButton

FOVButton = createToggle(
	"⭕ FOV : ON",
	165,
	function()

		FOVEnabled = not FOVEnabled

		-- TIDAK DIHAPUS.
		-- Hanya dibuat invisible.
		if FOVEnabled then

			FOVStroke.Transparency = 0
			FOVButton.Text = "⭕ FOV : ON"

		else

			FOVStroke.Transparency = 1
			FOVButton.Text = "⭕ FOV : OFF"

		end
	end
)

--==================================================
-- AIM STRENGTH LABEL
--==================================================

local AimLabel = Instance.new("TextLabel")
AimLabel.Size = UDim2.new(1,-30,0,20)
AimLabel.Position = UDim2.fromOffset(15,215)
AimLabel.BackgroundTransparency = 1
AimLabel.Text = "Aim Strength : 70%"
AimLabel.TextColor3 = Color3.new(1,1,1)
AimLabel.TextSize = 13
AimLabel.Font = Enum.Font.GothamBold
AimLabel.TextXAlignment = Enum.TextXAlignment.Left
AimLabel.Parent = Main

addContent(AimLabel)

--==================================================
-- AIM SLIDER
--==================================================

local AimSlider = Instance.new("Frame")
AimSlider.Size = UDim2.new(1,-30,0,10)
AimSlider.Position = UDim2.fromOffset(15,240)
AimSlider.BackgroundColor3 = Color3.fromRGB(55,55,65)
AimSlider.Parent = Main

local AimSliderCorner = Instance.new("UICorner")
AimSliderCorner.CornerRadius = UDim.new(1,0)
AimSliderCorner.Parent = AimSlider

addContent(AimSlider)

local AimFill = Instance.new("Frame")
AimFill.Size = UDim2.fromScale(AimStrength,1)
AimFill.BackgroundColor3 = Color3.fromRGB(0,220,255)
AimFill.BorderSizePixel = 0
AimFill.Parent = AimSlider

local AimFillCorner = Instance.new("UICorner")
AimFillCorner.CornerRadius = UDim.new(1,0)
AimFillCorner.Parent = AimFill

--==================================================
-- UNIVERSAL SLIDER FUNCTION
--==================================================

local function getSliderValue(input, slider)

	local x =
		(input.Position.X - slider.AbsolutePosition.X)
		/ slider.AbsoluteSize.X

	return math.clamp(x,0,1)
end

local function setupSlider(slider, callback)

	local active = false

	slider.InputBegan:Connect(function(input)

		if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

			active = true
			callback(getSliderValue(input,slider))
		end
	end)

	UIS.InputChanged:Connect(function(input)

		if not active then
			return
		end

		if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then

			callback(getSliderValue(input,slider))
		end
	end)

	UIS.InputEnded:Connect(function(input)

		if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

			active = false
		end
	end)
end

setupSlider(AimSlider,function(value)

	AimStrength = value

	AimFill.Size =
		UDim2.fromScale(value,1)

	AimLabel.Text =
		"Aim Strength : " ..
		math.floor(value*100) ..
		"%"
end)

--==================================================
-- FOV LABEL
--==================================================

local FOVLabel = Instance.new("TextLabel")
FOVLabel.Size = UDim2.new(1,-30,0,20)
FOVLabel.Position = UDim2.fromOffset(15,265)
FOVLabel.BackgroundTransparency = 1
FOVLabel.Text = "FOV Size : 180"
FOVLabel.TextColor3 = Color3.new(1,1,1)
FOVLabel.TextSize = 13
FOVLabel.Font = Enum.Font.GothamBold
FOVLabel.TextXAlignment = Enum.TextXAlignment.Left
FOVLabel.Parent = Main

addContent(FOVLabel)

--==================================================
-- FOV SLIDER
--==================================================

local FOVSlider = Instance.new("Frame")
FOVSlider.Size = UDim2.new(1,-30,0,10)
FOVSlider.Position = UDim2.fromOffset(15,290)
FOVSlider.BackgroundColor3 = Color3.fromRGB(55,55,65)
FOVSlider.Parent = Main

local FOVSliderCorner = Instance.new("UICorner")
FOVSliderCorner.CornerRadius = UDim.new(1,0)
FOVSliderCorner.Parent = FOVSlider

addContent(FOVSlider)

local FOVFill = Instance.new("Frame")
FOVFill.Size = UDim2.fromScale(
	(FOVSize-MinFOV)/(MaxFOV-MinFOV),
	1
)
FOVFill.BackgroundColor3 = Color3.fromRGB(0,220,255)
FOVFill.BorderSizePixel = 0
FOVFill.Parent = FOVSlider

local FOVFillCorner = Instance.new("UICorner")
FOVFillCorner.CornerRadius = UDim.new(1,0)
FOVFillCorner.Parent = FOVFill

setupSlider(FOVSlider,function(value)

	FOVSize =
		MinFOV +
		(MaxFOV-MinFOV)*value

	-- UKURAN LINGKARAN BENAR-BENAR BERUBAH
	FOV.Size =
		UDim2.fromOffset(FOVSize,FOVSize)

	FOVLabel.Text =
		"FOV Size : " ..
		math.floor(FOVSize)

	FOVFill.Size =
		UDim2.fromScale(value,1)
end)

--==================================================
-- CREDIT
--==================================================

local Credit = Instance.new("TextLabel")
Credit.Size = UDim2.new(1,-30,0,25)
Credit.Position = UDim2.fromOffset(15,325)
Credit.BackgroundTransparency = 1
Credit.Text = "⚡ BY CHALLO_BOY ⚡"
Credit.TextColor3 = Color3.new(1,1,1)
Credit.TextSize = 12
Credit.Font = Enum.Font.GothamBold
Credit.Parent = Main

addContent(Credit)

--==================================================
-- MINIMIZE SYSTEM
--==================================================

local minimized = false

Minimize.MouseButton1Click:Connect(function()

	minimized = not minimized

	if minimized then

		-- Sembunyikan isi, BUKAN menghancurkannya
		for _,obj in ipairs(Content) do
			obj.Visible = false
		end

		Main.Size = UDim2.fromOffset(165,50)

		Title.Text = "⚡ CHALLO_BOY"
		Title.Size = UDim2.new(1,-50,1,0)
		Title.Position = UDim2.fromOffset(12,0)
		Title.TextSize = 14

		Minimize.Position =
			UDim2.new(1,-43,0,10)

		Minimize.Text = "+"

	else

		for _,obj in ipairs(Content) do
			obj.Visible = true
		end

		Main.Size = UDim2.fromOffset(250,365)

		Title.Text = "⚡ AIM ASSIST"
		Title.Size = UDim2.new(1,-55,0,38)
		Title.Position = UDim2.fromOffset(15,8)
		Title.TextSize = 20

		Minimize.Text = "—"
	end
end)

--==================================================
-- RGB
--==================================================

local hue = 0

RunService.RenderStepped:Connect(function(dt)

	hue =
		(hue + dt*0.25)%1

	local rgb =
		Color3.fromHSV(hue,1,1)

	FOVStroke.Color = rgb
	MainStroke.Color = rgb

	AimFill.BackgroundColor3 = rgb
	FOVFill.BackgroundColor3 = rgb

	Credit.TextColor3 = rgb
end)

--==================================================
-- ESP OUTLINE
--==================================================

local highlights = {}

local function removeESP(player)

	if highlights[player] then

		highlights[player]:Destroy()
		highlights[player] = nil

	end
end

local function addESP(player)

	if player == LocalPlayer then
		return
	end

	local function setup(character)

		removeESP(player)

		local highlight = Instance.new("Highlight")

		highlight.Name = "ChalloBoyESP"
		highlight.Adornee = character

		-- OUTLINE TETAP TERLIHAT MELALUI OBJEK
		highlight.DepthMode =
			Enum.HighlightDepthMode.AlwaysOnTop

		highlight.FillTransparency = 1
		highlight.OutlineTransparency = 0
		highlight.Enabled = ESPEnabled

		highlight.Parent = character

		highlights[player] = highlight
	end

	if player.Character then
		setup(player.Character)
	end

	player.CharacterAdded:Connect(function(character)

		task.wait(0.5)

		setup(character)
	end)
end

for _,player in ipairs(Players:GetPlayers()) do
	addESP(player)
end

Players.PlayerAdded:Connect(addESP)

Players.PlayerRemoving:Connect(function(player)

	removeESP(player)
end)

--==================================================
-- UPDATE ESP
--==================================================

RunService.RenderStepped:Connect(function()

	local rgb =
		Color3.fromHSV(hue,1,1)

	for player,highlight in pairs(highlights) do

		if highlight and highlight.Parent then

			highlight.Enabled =
				ESPEnabled

			highlight.OutlineColor =
				rgb
		end
	end
end)

--==================================================
-- LINE OF SIGHT
--==================================================

local function IsVisible(character)

	local root =
		character:FindFirstChild("HumanoidRootPart")

	if not root then
		return false
	end

	local origin =
		Camera.CFrame.Position

	local direction =
		root.Position-origin

	local params =
		RaycastParams.new()

	params.FilterType =
		Enum.RaycastFilterType.Exclude

	params.FilterDescendantsInstances = {
		LocalPlayer.Character,
		character
	}

	local result =
		workspace:Raycast(
			origin,
			direction,
			params
		)

	return result == nil
end

--==================================================
-- TARGET
--==================================================

local function GetTarget()

	local bestTarget = nil
	local bestDistance = math.huge

	local viewport =
		Camera.ViewportSize

	local center =
		Vector2.new(
			viewport.X/2,
			viewport.Y/2
		)

	for _,player in ipairs(Players:GetPlayers()) do

		if player ~= LocalPlayer then

			local character =
				player.Character

			if character then

				local humanoid =
					character:FindFirstChildOfClass("Humanoid")

				local root =
					character:FindFirstChild("HumanoidRootPart")

				if humanoid and root
				and humanoid.Health > 0 then

					local worldDistance =
						(
							Camera.CFrame.Position
							-root.Position
						).Magnitude

					if worldDistance <= MaxDistance then

						local screenPos,onScreen =
							Camera:WorldToViewportPoint(
								root.Position
							)

						if onScreen then

							local screenDistance =
								(
									Vector2.new(
										screenPos.X,
										screenPos.Y
									)-center
								).Magnitude

							if screenDistance <= FOVSize/2 then

								if IsVisible(character) then

									if screenDistance < bestDistance then

										bestTarget = root
										bestDistance =
											screenDistance
									end
								end
							end
						end
					end
				end
			end
		end
	end

	return bestTarget
end

--==================================================
-- AIM
--==================================================

RunService.RenderStepped:Connect(function()

	if not AimEnabled then
		return
	end

	if AimStrength <= 0 then
		return
	end

	local target =
		GetTarget()

	if target then

		local targetCFrame =
			CFrame.lookAt(
				Camera.CFrame.Position,
				target.Position
			)

		Camera.CFrame =
			Camera.CFrame:Lerp(
				targetCFrame,
				AimStrength
			)
	end
end)
